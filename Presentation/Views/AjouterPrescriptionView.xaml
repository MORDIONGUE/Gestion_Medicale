<Window x:Class="A_C.Presentation.Views.AjouterPrescriptionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ajouter une Prescription" Height="450" Width="600"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Nouvelle Prescription" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Médicament :"/>
            <TextBox x:Name="MedicamentTextBox"/>
            
            <TextBlock Text="Dosage :"/>
            <TextBox x:Name="DosageTextBox"/>
            
            <TextBlock Text="Instructions :"/>
            <TextBox x:Name="InstructionsTextBox" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
            
            <TextBlock Text="Durée :"/>
            <TextBox x:Name="DureeTextBox"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Annuler" Width="100" Click="Annuler_Click" Margin="0,0,10,0"/>
            <Button Content="Enregistrer" Width="100" Click="Enregistrer_Click" Background="#4CAF50" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window> 